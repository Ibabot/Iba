version: '3.0'
services:
  rasa:
    image: rasa/rasa:latest-full
    ports:
      - 5006:5006
    volumes:
      - ./:/app
    command:
      - run
      - --cors
      - "*"
  action_server:
    image: rasa/rasa-sdk:latest
    ports:
    - "5056:5056"
    volumes:
      - ./actions:/app/actions
