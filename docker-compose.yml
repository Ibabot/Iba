version: '3.0'
services:
  rasa:
    image: rasa/rasa:1.5.1
    ports:
      - 5006:5006
    volumes:
      - ./:/app
    command:
      - run
      - --cors
      - "*"
  action_server:
    image: rasa/rasa-sdk:1.5.1
    ports:
    - "5056:5056"
    volumes:
      - ./actions:/app/actions
